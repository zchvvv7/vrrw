# 车载视觉道路障碍物与未知风险预警系统 — 技术参数文档

> 文档版本：v1.0  
> 生成日期：2026-08-15  
> 适用项目：vehicle-road-risk-warning

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构与流水线](#2-系统架构与流水线)
3. [配置管理体系](#3-配置管理体系)
4. [核心模块详解](#4-核心模块详解)
   - 4.1 [RoadSegmenter — 可行驶区域分割模块](#41-roadsegmenter--可行驶区域分割模块)
   - 4.2 [KnownDetector — 已知障碍物检测模块](#42-knowndetector--已知障碍物检测模块)
   - 4.3 [DistanceEstimator — 距离估计模块](#43-distanceestimator--距离估计模块)
   - 4.4 [UnknownDetector — 未知障碍物检测模块](#44-unknowndetector--未知障碍物检测模块)
   - 4.5 [Mask2AnomalyBackend — 异常检测后端](#45-mask2anomalybackend--异常检测后端)
   - 4.6 [CorridorPredictor — 行驶走廊预测模块](#46-corridorpredictor--行驶走廊预测模块)
   - 4.7 [RiskEvaluator — 风险评估模块](#47-riskevaluator--风险评估模块)
5. [数据输入模块](#5-数据输入模块)
6. [可视化模块](#6-可视化模块)
7. [统一数据结构（Schemas）](#7-统一数据结构schemas)
8. [模块间接口（Interfaces）](#8-模块间接口interfaces)
9. [项目优缺点总览](#9-项目优缺点总览)
10. [依赖与环境](#10-依赖与环境)

---

## 1. 项目概述

本项目是一个**车载视觉道路风险预警系统**，通过单目前视视频实现以下核心能力：

- **可行驶区域分割**：识别图像中的道路区域
- **已知障碍物检测**：检测锥桶、护栏、坑洞、车辆等已知类别障碍物
- **未知障碍物检测**：发现道路区域内的未知异常物体
- **距离估计**：基于几何投影方法估算障碍物距离
- **行驶走廊预测**：预测自车在道路上的可行驶走廊
- **风险评估**：综合障碍物位置、距离、TTC等信息输出风险等级

系统最终输出 `safe / notice / warning / danger` 四级风险预警，以及带标注的可视化视频和 JSON 格式的详细结果记录。

---

## 2. 系统架构与流水线

### 2.1 整体架构

```
┌────────────────────────────────────────────────────────────────┐
│                        main.py                                 │
│                   （流水线主控器）                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌───────────────┐    ┌─────────────┐    ┌─────────────────┐   │
│  │ DataReader    │───▶│  Pipeline   │───▶│ ResultVisualizer│   │
│  │(Video/Camera) │    │  (逐帧循环)  │    │  (绘制+保存)      │   │
│  └───────────────┘    └──────┬──────┘    └─────────────────┘   │
│                              │                                 │
│           ┌──────────────────┼──────────────────┐              │
│           ▼                  ▼                  ▼              │
│    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│    │RoadSegmenter │  │KnownDetector │  │UnknownDetect │        │
│    │ (SegFormer)  │  │   (YOLO)     │  │(Mask2Anomaly)│        │
│    └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│           │                 │                 │                │
│           ▼                 ▼                 │                │
│    ┌──────────────┐  ┌──────────────┐         │                │
│    │DistanceEst.  │  │CorridorPred. │         │                │
│    │  (几何投影)   │  │ (道路几何)     │         │                │
│    └──────┬───────┘  └──────┬───────┘         │                │
│           │                 │                 │                │
│           └─────────┬───────┘─────────────────┘                │
│                     ▼                                          │
│              ┌──────────────┐                                  │
│              │RiskEvaluator │                                  │
│              │  (TTC+空间)   │                                  │ 
│              └──────────────┘                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 2.2 单帧处理流水线

每帧图像按以下顺序流经各模块：

| 步骤 | 模块 | 输入 | 输出 |
|------|------|------|------|
| 1 | RoadSegmenter | 原始帧 | `RoadSegmentResult` (道路掩码) |
| 2 | KnownDetector | 原始帧 | `KnownDetectionResult` (已知障碍物列表) |
| 3 | DistanceEstimator | 帧 + 已知障碍物 | `DistanceEstimationResult` (含距离的障碍物) |
| 4 | UnknownDetector | 帧 + 道路掩码 + 已知障碍物 | `UnknownDetectionResult` (异常区域) |
| 5 | DistanceEstimator | 帧 + 异常区域 | 更新后的 `UnknownRegion` (含距离) |
| 6 | CorridorPredictor | 帧 + 道路掩码 | `CorridorPredictionResult` (走廊掩码) |
| 7 | RiskEvaluator | 所有结果 + FPS | `RiskEvaluationResult` (风险等级) |

### 2.3 运行模式

- **视频文件模式**：逐帧读取本地视频文件，支持 `frame_skip` 跳帧
- **摄像头模式**：实时从本地摄像头采集，支持实时弹窗显示

---

## 3. 配置管理体系

### 3.1 配置层级

```
configs/default.yaml            ← 主配置（所有模块参数）
    │
    ├── configs/road_segmenter.yaml        ← 道路分割专用配置
    │
    └── configs/mask2anomaly/              ← 异常检测模型配置
        ├── Base-Cityscapes-SemanticSegmentation.yaml
        └── anomaly_inference.yaml
```

### 3.2 配置加载方式

| 配置文件 | 加载方式 | 数据结构 |
|---------|---------|---------|
| `default.yaml` | `yaml.safe_load()` → `dict` | 各模块独立 key |
| `road_segmenter.yaml` | `RoadSegmenterConfig.from_yaml()` → `dataclass` | 7 个嵌套 dataclass |
| `anomaly_inference.yaml` | Detectron2 `CfgNode.merge_from_file()` | Detectron2 配置体系 |

### 3.3 RoadSegmenter 配置数据类

```python
RoadSegmenterConfig
├── ModelConfig          # 模型名称、Checkpoint路径、类别数
├── DataConfig           # 输入尺寸、归一化参数
├── LabelsConfig         # 数据集类型、道路标签映射
├── PostProcessingConfig # 置信度阈值、边界检测、平滑参数
├── PerformanceConfig    # 设备选择、性能分析
├── QualityConfig        # 亮度/模糊度阈值、降级判定
└── SystemConfig         # 日志级别、错误码定义
```

### 3.4 可配置开关

以下模块支持通过 `enabled` 字段独立开关：

| 模块 | 配置键 | 默认值 | 关闭后行为 |
|------|--------|--------|-----------|
| DistanceEstimator | `distance_estimator.enabled` | `true` | 障碍物距离为 `None` |
| CorridorPredictor | `corridor_predictor.enabled` | `true` | 走廊掩码为空 |
| RiskEvaluator | `risk_evaluator.enabled` | `true` | 风险等级固定为 `safe` |

### 3.5 参数默认值约定

> **本文档中"默认值"列的含义**：
> - 如参数在 `default.yaml` 中有配置 → 报告 YAML 中的配置值（运行时实际使用的值）
> - 如参数仅在代码中有默认值 → 报告代码中的 `config.get(key, default)` 默认值
> - 标注 `(default.yaml 配置值)` 表示该值来自配置文件，而非代码默认值

---

## 4. 核心模块详解

### 4.1 RoadSegmenter — 可行驶区域分割模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/road_segmenter.py` |
| 配置文件 | `configs/road_segmenter.yaml` |
| 模型 | **SegFormer-B2** (NVIDIA/segformer-b2-finetuned-cityscapes-1024-1024) |
| 框架 | HuggingFace `transformers.SegformerForSemanticSegmentation` |
| 预训练权重 | 3 种来源：HuggingFace Hub / 本地 .pth 文件 / Cityscapes |
| 类别数 | 19 类 (Cityscapes) |
| 道路标签 | Cityscapes 标签 ID = 0 |

#### 模型架构

SegFormer 基于 **Vision Transformer (ViT)** 架构，使用 **Mix Transformer (MiT)** 作为骨干网络：

- **Backbone**：MiT-B2，包含 4 个 Transformer Encoder 块
- **Encoder**：层次化特征提取，输出多尺度特征图
- **Decoder**：MLP 解码器，融合多尺度特征进行像素级分类
- **输入分辨率**：1024 × 1024
- **输出分辨率**：双线性上采样到原始帧尺寸

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `model_name` | str | `"nvidia/segformer-b2-finetuned-cityscapes-1024-1024"` | HuggingFace 模型名 |
| `checkpoint_path` | str / None | `None` | 本地 .pth 权重路径 |
| `num_classes` | int | 19 | 分割类别数 |
| `input_size` | list[int] | `[1024, 1024]` | 模型输入尺寸 |
| `divisor` | int | 32 | 填充整除因子 |
| `mean` | list[float] | `[0.485, 0.456, 0.406]` | ImageNet 归一化均值 |
| `std` | list[float] | `[0.229, 0.224, 0.225]` | ImageNet 归一化标准差 |
| `confidence_threshold` | float | 0.5 | 道路像素置信度阈值 |
| `boundary_method` | str | `"canny"` | 边界检测方法 (canny/sobel/contour) |
| `smoothing_kernel_size` | int | 5 | 形态学平滑核大小 |
| `spline_order` | int | 3 | B样条阶数 |
| `brightness_low_threshold` | int | 20 | 低亮度降级阈值 |
| `brightness_high_threshold` | int | 230 | 高亮度降级阈值 |
| `blur_threshold` | float | 30.0 | 拉普拉斯方差模糊阈值 |
| `degraded_confidence_threshold` | float | 0.1 | 置信度降级阈值 |
| `unavailable_confidence_threshold` | float | 0.05 | 置信度不可用阈值 |

#### 处理流程

1. **输入验证** → 检查帧是否为 3 通道 uint8 numpy 数组
2. **图像质量检查** → 计算亮度 (灰度均值) 和模糊度 (拉普拉斯方差)
   - 过暗 (< 30) → `UNAVAILABLE`
   - 过亮 (> 220) / 低亮度 / 模糊 → `DEGRADED`
3. **模型推理** → SegFormer 前向传播
   - BGR→RGB 转换
   - `SegformerImageProcessor` 预处理
   - `torch.no_grad()` 推理
   - Softmax → 双线性上采样 → Argmax
4. **后处理**：
   - 提取道路类别 (label=0) 生成二值掩码
   - 形态学闭运算 + 开运算 + 高斯模糊
   - B 样条平滑轮廓
   - Canny 边界检测
   - 计算全局置信度 (置信度≥0.5 的道路像素占比)
5. **降级判定**：
   - 全局置信度 < 0.1 → `DEGRADED`
   - 全局置信度 < 0.05 → `UNAVAILABLE`

#### 输出数据结构

```python
RoadSegmentResult(
    mask: np.ndarray              # 道路二值掩码 (uint8, 0/255)
    boundary: Optional[np.ndarray] # 边界线掩码
    confidence_map: np.ndarray     # 每个像素属于道路的概率
    global_confidence: float       # 全局道路置信度
    road_pixel_ratio: float        # 道路像素占比
    error_code: int                # 错误码 (0=成功)
    error_message: str             # 错误信息
    inference_time_ms: float       # 推理耗时
    system_status: str             # normal/degraded/unavailable
    quality_metrics: dict          # 亮度、模糊度等质量指标
)
```

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_init_device()` | 自动选择 CUDA/CPU 设备 |
| `_initialize_model()` | 加载 SegFormer 模型和处理器 |
| `_pad_to_divisible()` | 填充图像使其能被 divisor 整除 |
| `_detect_boundary()` | 检测道路边界 (Canny/Sobel/轮廓) |
| `_smooth_mask()` | 形态学平滑 + 高斯模糊 |
| `_smooth_contour()` | B 样条轮廓平滑 |
| `_validate_input()` | 输入帧有效性校验 |
| `_calculate_brightness()` | 计算灰度均值亮度 |
| `_calculate_blur()` | 计算拉普拉斯方差模糊度 |
| `_check_image_quality()` | 综合质量检查 → 降级判定 |
| `predict()` | **主入口**：执行完整分割流程 |
| `get_model_info()` | 获取模型元信息 |
| `load_checkpoint()` | 加载本地 .pth 权重文件 |

#### 优点

- ✅ **SegFormer 架构先进**：基于 Transformer，在 Cityscapes 上精度高 (mIoU ~82%)
- ✅ **多级降级机制**：支持 normal/degraded/unavailable 三档状态
- ✅ **图像质量前置检查**：亮度和模糊度检测可提前发现无效帧
- ✅ **丰富的后处理**：形态学平滑 + B 样条曲线拟合，输出掩码平滑
- ✅ **HuggingFace 集成**：直接从 Hub 加载预训练模型，使用方便
- ✅ **多数据集标签映射**：支持 Cityscapes / BDD100K / Mapillary

#### 缺点

- 🟡 **无实时性能**：SegFormer-B2 在 CPU 上推理较慢 (~200-500ms/帧)
- 🟡 **固定输入尺寸**：模型以 1024×1024 输入，小目标可能丢失
- 🟡 **仅支持道路分割**：不提供车道线检测或可行驶区域边界线
- 🟡 **依赖 torch>=2.0**：对环境版本要求较高
- 🟡 **无动态 batch**：逐帧单张推理，无法利用批量加速

---

### 4.2 KnownDetector — 已知障碍物检测模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/known_detector.py` |
| 配置来源 | `configs/default.yaml` → `known_detector` 段 |
| 模型 | **YOLOv8** (Ultralytics YOLO) |
| 框架 | `ultralytics.YOLO` |
| 预训练权重 | `checkpoints/yolo_best.pt` (自训练模型) |
| 支持类别 | 4 类：cone, barrier, pothole, vehicle |

#### 模型架构

YOLOv8 是单阶段目标检测模型，采用 C2f 模块和 Decoupled Head：

- **Backbone**：CSPDarknet，使用 C2f (Cross Stage Partial Bottleneck with 2 convolutions) 模块
- **Neck**：PANet (Path Aggregation Network)，FPN + 自顶向下路径
- **Head**：Decoupled Head，分类和回归分支分离
- **损失函数**：CIoU Loss + BCE Loss (分类) + DFL (Distribution Focal Loss)

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `model_name` | str | `"checkpoints/yolo_best.pt"` | 模型权重路径 (项目相对路径) |
| `conf_threshold` | float | 0.25 | 置信度阈值 |
| `iou_threshold` | float | 0.45 | NMS IoU 阈值 |
| `device` | str / int | `"auto"` | 推理设备 (auto/cpu/0) |
| `classes` | list[str] | `[cone, barrier, pothole, vehicle]` | 目标类别白名单 |
| `weights_sha256` | str | - | SHA256 权重校验值 (64 字符) |

#### 处理流程

1. **配置校验**：
   - `conf_threshold` ∈ [0, 1]
   - `iou_threshold` ∈ [0, 1]
   - `device` 合法性检查
   - `classes` 非空检查
   - `weights_sha256` 长度检查 (64 字符)
2. **路径解析**：将项目相对路径解析为绝对路径，验证文件在项目根目录内
3. **权重校验**：SHA256 哈希比对，确保权重文件完整无损
4. **类别校验**：验证配置中的 `classes` 是否都存在于模型 `names` 字典中
5. **模型初始化**：`YOLO(model_path)` 加载权重
6. **推理检测**：
   - `model.predict(source=frame, conf=0.25, iou=0.45)`
   - 结果转换：YOLO Box → `DetectedObject`
   - 类别过滤：只保留白名单内的类别

#### 输出数据结构

```python
KnownDetectionResult(
    objects: List[DetectedObject]    # 检测到的障碍物列表
    inference_time_ms: float         # 推理耗时
    error_code: int                  # 错误码 (0=成功)
    error_message: str               # 错误信息
    model_version: str               # 模型版本字符串 (yolo-{name}-{sha256[:12]})
)
```

其中每个 `DetectedObject`：
```python
DetectedObject(
    class_name: str                  # 类别名 (cone/barrier/pothole/vehicle)
    bbox: Tuple[int, int, int, int]  # 边界框 (x1, y1, x2, y2)
    confidence: float                # 检测置信度
    distance: Optional[float]        # 距离 (后续由 DistanceEstimator 填充)
)
```

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_initialize_model()` | 完整初始化流程 (校验→路径→权重→加载) |
| `_validate_configuration()` | 校验所有配置参数合法性 |
| `_resolve_model_path()` | 解析项目相对路径为绝对路径 |
| `_calculate_sha256()` | 分块计算文件 SHA256 哈希 |
| `_verify_weights()` | SHA256 校验权重文件完整性 |
| `_validate_model_classes()` | 校验配置类别在模型中存在 |
| `_validate_frame()` | 输入帧格式校验 |
| `_convert_detections()` | YOLO 输出 → 统一数据结构 |
| `predict()` | **主入口**：执行检测流程 |

#### 优点

- ✅ **YOLOv8 先进架构**：C2f 模块精度高、速度快
- ✅ **SHA256 权重校验**：防止权重文件损坏或被篡改
- ✅ **严格的路径安全**：限制模型路径必须在项目根目录内
- ✅ **类别白名单**：灵活控制检测目标类别
- ✅ **明确的错误处理**：初始化失败时记录具体异常类型
- ✅ **Ultralytics 生态**：支持多种导出格式 (ONNX/TensorRT 等)

#### 缺点

- 🟡 **仅 4 个类别**：自训练模型类别有限，通用场景适用性差
- 🟡 **无跟踪能力**：逐帧独立检测，不关联历史目标
- 🟡 **固定阈值**：conf_threshold 和 iou_threshold 全局统一，无法针对类别调整
- 🟡 **依赖自训练权重**：需要用户自行训练或提供 .pt 文件
- 🟡 **类名全小写处理**：对大小写敏感的类别名可能出错

---

### 4.3 DistanceEstimator — 距离估计模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/distance_estimator.py` |
| 配置来源 | `configs/default.yaml` → `distance_estimator` 段 |
| 算法 | **几何投影距离估计** (基于相机内参 + 目标真实高度) |
| 实现接口 | `DistanceEstimatorInterface` (抽象基类) |

#### 核心算法

距离估计采用两种互补方法：

**方法一：基于目标真实高度（已知类别）**
```
distance = focal_length_px × real_height / bbox_height_px
```
适用于已知类别的障碍物，其中 `real_height` 根据类别预定义：
- cone (锥桶): 0.70 m
- barrier (护栏): 1.00 m
- vehicle (车辆): 1.50 m

**方法二：基于底部接地点投影（通用方法）**
```
distance = focal_length_px × camera_height_m / vertical_offset
```
其中 `vertical_offset = bbox_bottom_y - horizon_y`，`horizon_y = frame_height × horizon_ratio`
适用于未知障碍物或已知障碍物的回退方案。

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enabled` | bool | `true` | 模块开关 |
| `method` | str | `"geometric_bbox_height"` | 方法标识 |
| `focal_length_px` | float | 1000.0 | 相机焦距 (像素) |
| `min_bbox_height_px` | int | 8 | 最小边界框高度 (小于此值用接地点法) |
| `min_distance_m` | float | 0.3 | 有效距离下限 (米) |
| `max_distance_m` | float | 80.0 | 有效距离上限 (米) |
| `camera_height_m` | float | 1.50 | 相机离地高度 (米) |
| `horizon_ratio` | float | 0.50 | 地平线在画面中的位置比例 |
| `class_heights_m` | dict | `{cone:0.70, barrier:1.00, vehicle:1.50}` | 各类别真实高度 |

#### 处理流程（已知障碍物）

1. 遍历 `known_objects` 列表
2. 对每个障碍物：
   - 裁剪 bbox 到图像范围内
   - 若 bbox 高度 < `min_bbox_height_px` → 使用接地点法
   - 否则：查找类别真实高度 → 使用高度法
   - 若类别无预定义高度 → 回退到接地点法
   - 计算距离 → 检查是否在 `[min, max]` 范围内
3. 使用 `dataclasses.replace()` 给 DetectedObject 添加 distance 字段

#### 处理流程（未知障碍物）

1. 对每个 `UnknownRegion` 的 bbox：
   - 使用接地点投影法
   - `distance = focal × camera_height / (bottom_y - horizon_y)`
   - 检查有效性范围

#### 方法列表

| 方法 | 功能 |
|------|------|
| `estimate()` | **主入口**：估计已知障碍物距离 |
| `estimate_unknown_regions()` | 估计未知障碍物距离 (对外接口) |
| `_estimate_known_objects()` | 批量估计已知障碍物 |
| `_estimate_one_object()` | 单目标距离估计 (高度法) |
| `_estimate_ground_distance()` | 基于接地点的通用距离估计 |
| `_clip_bbox()` | 裁剪 bbox 到图像范围 |

#### 优点

- ✅ **纯几何方法**：无需额外模型，零推理开销
- ✅ **轻量高效**：纯数学计算，毫秒级完成
- ✅ **双方法互补**：高度法精度高 + 接地点法通用性强
- ✅ **可独立关闭**：通过 `enabled: false` 禁用
- ✅ **实现接口隔离**：`DistanceEstimatorInterface` 抽象基类便于替换

#### 缺点

- 🟡 **需要精确内参**：`focal_length_px` 依赖相机标定
- 🟡 **需要真实高度先验**：`class_heights_m` 需手动配置，精度依赖真实值
- 🟡 **平面假设**：接地点法假设平坦路面，坡道/弯道会失准
- 🟡 **无深度估计能力**：无法估计精确的三维空间距离
- 🟡 **对小目标失效**：bbox 高度过小时 (`< 8px`) 回退方案可能也不准确
- 🟡 **距离跳变**：逐帧 bbox 抖动会导致距离估计不稳定

---

### 4.4 UnknownDetector — 未知障碍物检测模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/unknown_detector.py` |
| 配置来源 | `configs/default.yaml` → `unknown_detector` 段 |
| 后端模型 | **Mask2Anomaly** (Mask2Former 变体 + 异常分数计算) |
| 实现功能 | 像素级异常检测 + 后处理过滤 |

#### 核心思想

利用 Mask2Former 的语义分割输出，将 **异常分数 = 1 - 最大已知类别概率**：

```
anomaly_score(x, y) = 1.0 - max(inlier_class_probabilities[:, x, y])
```

如果该像素不属于任何已知类别（inlier classes），则异常分数高，表明可能存在未知障碍物。

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `backend` | str | `"mask2anomaly"` | 后端名称 (目前唯一支持) |
| `pixel_threshold` | float | 0.5 | 像素级异常二值化阈值 |
| `min_area_ratio` | float | 0.0002 | 最小异常区域占图像面积比 |
| `max_area_ratio` | float | 0.15 | 最大异常区域占图像面积比 |
| `lower_roi_ratio` | float | 0.25 | ROI 起始位置 (画面下方 75%) |
| `roi_dilate_kernel_size` | int | 31 | ROI 膨胀核大小 |
| `morphology_kernel_size` | int | 5 | 形态学清理核大小 |
| `region_score_quantile` | float | 0.95 | 区域分数分位数 |
| `known_box_padding_ratio` | float | 0.1 | 已知障碍物排除框扩展比 |
| `known_box_min_padding` | int | 4 | 排除框最小扩展像素 |

#### 后处理流程

1. **模型推理** → 得到像素级异常分数图
2. **ROI 构建**：
   - 道路掩码膨胀 31×31 核
   - 与画面下方 75% 区域取交集
   - 只关注画面下半部的道路区域
3. **阈值化** → `score ≥ 0.5` → 二值候选掩码
4. **形态学清理** → 开运算 + 闭运算 (5×5 核)
5. **已知障碍物排除** → 在已知 bbox 周围 padding 区域内清零
6. **区域提取**：
   - `cv2.findContours` 查找连通域
   - 面积过滤：`[min_area, max_area]` 范围内
   - 分数计算：区域内像素值的 95% 分位数
   - 输出 `UnknownRegion` 列表 (含 bbox、score、RLE 掩码编码)

#### 输出数据结构

```python
UnknownDetectionResult(
    score_map: np.ndarray          # 像素级异常分数图 (float32, 0~1)
    anomaly_mask: np.ndarray       # 二值异常掩码 (uint8)
    regions: List[UnknownRegion]   # 异常区域列表
    inference_time_ms: float       # 推理耗时
    error_code: int                # 错误码
    error_message: str             # 错误信息
    model_version: str             # 模型版本字符串
)
```

其中每个 `UnknownRegion`：
```python
UnknownRegion(
    bbox: Tuple[int, int, int, int]  # 边界框
    score: float                      # 区域异常分数
    distance: Optional[float]         # 距离 (后续填充)
    object_id: Optional[str]          # 唯一 ID (unknown-000, ...)
    area: Optional[int]               # 区域面积 (像素)
    mask_rle: Optional[dict]          # RLE 编码的像素掩码
)
```

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_initialize_backend()` | 初始化 Mask2Anomaly 后端 |
| `_build_road_roi()` | 构建道路 ROI (膨胀+下方区域) |
| `_build_known_exclusion_mask()` | 构建已知障碍物排除掩码 |
| `_clean_candidate_mask()` | 形态学清理候选掩码 |
| `_encode_mask_rle()` | RLE 编码二值掩码 (列优先) |
| `_mask_to_unknown_regions()` | 连通域分析 → 区域列表 |
| `predict()` | **主入口**：完整异常检测流程 |

#### 优点

- ✅ **无监督异常检测**：不需要未知障碍物的训练样本
- ✅ **像素级精度**：输出像素级异常掩码，而非仅 bbox
- ✅ **已知障碍物排除**：避免将已知障碍物误报为未知
- ✅ **ROI 聚焦**：只关注道路区域下方，减少误报
- ✅ **面积过滤**：排除过大/过小区域，提高鲁棒性
- ✅ **RLE 编码**：压缩掩码数据，便于 JSON 存储
- ✅ **分位数评分**：使用 95% 分位数而非均值，抗异常值干扰

#### 缺点

- 🟡 **严重依赖 CUDA**：Mask2Anomaly 需要 Detectron2 + CUDA 扩展
- 🟡 **环境配置复杂**：需要编译 MSDeformAttn CUDA 扩展
- 🟡 **推理速度慢**：Detectron2 模型 + CUDA 推理 (~200-400ms/帧)
- 🟡 **异常 = 未知**的假设不一定成立：模型未见过的类别可能是噪声
- 🟡 **仅输出分数**：无法区分异常类型，需要人工判断
- 🟡 **min_area_ratio 很小 (0.02%)**：可能检测到非常小的噪声区域

---

### 4.5 Mask2AnomalyBackend — 异常检测后端

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/mask2anomaly_backend.py` |
| 模型 | **Mask2Former with Anomaly Head** (ResNet-50 + MaskFormer) |
| 框架 | Detectron2 + Mask2Former |
| 预训练权重 | `checkpoints/mask2anomaly/best_contrastive_fp16.pth` |
| Backbone | ResNet-50 (ImageNet 预训练) |

#### 模型架构

**完整架构：ResNet-50 + MSDeformAttn PixelDecoder + MultiScaleMaskedTransformerDecoder_GMA**

```
输入图像
    │
    ▼
┌──────────────┐
│  ResNet-50   │ → 多尺度特征 [res2, res3, res4, res5]
│  (Backbone)  │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ MSDeformAttnPixelDec.│ → 多尺度像素特征 (256 维)
│  (可变形注意力)       │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│ MultiScaleMaskedTransformerDecoder_GMA       │
│  (Global Cross Attention 解码器)             │
│  - 10 层 Transformer Decoder                 │
│  - 每层使用 GlobalCrossAttentionLayer        │
│  - 100 个 learnable object queries           │
│  - 8 头注意力, hidden_dim=256                │
│  - 3 尺度特征 (levels)                       │
└──────┬───────────────────────────────────────┘
       │
       ▼
  语义分割输出 (19 类 inlier + extra 类)
       │
       ▼
  异常分数 = 1 - max(inlier_scores)
```

**GMA (Global Cross Attention) 机制详解：**

GMA 是对标准 Mask2Former 解码器的关键修改。标准解码器使用单一的 `CrossAttentionLayer`，而 GMA 版本使用 `GlobalCrossAttentionLayer`，包含**两个并行的注意力分支**：

| 分支 | 注意力掩码 | 关注区域 | 作用 |
|------|-----------|---------|------|
| **Foreground Attention** | `sigmoid(mask) < 0.5` | 仅物体区域（预测的 mask 内） | 捕捉已知类别的精确特征 |
| **Background Attention** | `sigmoid(mask) > 0.5` | 仅背景区域（预测的 mask 外） | 捕捉未知/异常的上下文信息 |

两个分支的输出**相加融合** (`tgt = foreground + background`)，使得模型能够同时关注已知物体（前景）和周围环境（背景），从而更好地检测分布外（OOD）异常。

在每一层解码中，模型先进行 Cross Attention（与编码特征交互），再进行 Self Attention（query 间交互），最后通过 FFN 变换。每层预测头都会输出中间结果用于辅助损失计算。

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `device` | str | `"cuda:0"` | 推理设备 |
| `num_inlier_classes` | int | 19 | 已知 (inlier) 类别数 |
| `known_mask_threshold` | float | 0.5 | 已知掩码阈值 (过滤 extra 类) |
| `enable_flip_tta` | bool | `false` | 是否启用水平翻转 TTA |
| `weights_sha256` | str | - | 64 字符 SHA256 校验值 |

#### 异常分数计算

```python
# sem_seg: [C, H, W] — Detectron2 MaskFormer 输出的语义分割分数图
# 注意：sem_seg 值已在 [0, 1] 范围内（经过 MaskFormer 内置后处理）

inlier_scores = sem_seg[:num_inlier_classes]      # 前 19 个通道（已知类别）
anomaly_score = 1.0 - inlier_scores.amax(dim=0)    # 1 - 最大已知类别分数

extra_masks = sem_seg[num_inlier_classes:]          # 额外通道（训练时的"未知"类）
known_mask = extra_masks.amax(dim=0)               # 额外通道的最大分数
keep_mask = (known_mask < known_mask_threshold)    # 过滤掉被判为"已知"的额外类
anomaly_score = anomaly_score * keep_mask           # 最终异常分数

# 最终 clamp 到 [0, 1] 范围
anomaly_score = anomaly_score.clamp(0.0, 1.0)
```

**核心思想**：如果一个像素不属于任何已知类别（inlier classes），其异常分数就高；如果被额外通道的高分覆盖（可能被模型判为某类"未知"已知模式），则被抑制。

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_resolve_project_file()` | 解析项目相对路径 |
| `_validate_configuration()` | 校验 CUDA/参数 |
| `_verify_weights()` | SHA256 权重校验 |
| `_build_predictor()` | 构建 Detectron2 `DefaultPredictor` |
| `_create_anomaly_score()` | 语义分割 → 异常分数图 |
| `_predict_single()` | 单帧推理 |
| `predict()` | **主入口**：推理 + (可选) TTA + 计时 |

#### 优点

- ✅ **最先进的异常检测方法**：基于 Mask2Former + 对比学习
- ✅ **GMA (Unknown-aware) 训练**：专门针对开放集检测优化
- ✅ **多尺度特征**：MSDeformAttn 捕捉不同尺度的异常
- ✅ **可配置 TTA**：水平翻转 TTA 可进一步提升精度
- ✅ **SHA256 权重校验**：与 KnownDetector 保持一致的安全机制

#### 缺点

- 🟡 **极其复杂的依赖**：Detectron2 + CUDA + MSDeformAttn 编译链
- 🟡 **FP16 权重**：需要硬件支持半精度计算
- 🟡 **仅支持 GPU**：配置强制要求 `cuda:0`
- 🟡 **推理速度慢**：ResNet-50 + Transformer Decoder 计算量大
- 🟡 **训练数据封闭**：仅在 Cityscapes 分布内验证有效

---

### 4.6 CorridorPredictor — 行驶走廊预测模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/corridor_predictor.py` |
| 配置来源 | `configs/default.yaml` → `corridor_predictor` 段 |
| 算法 | **道路掩码几何分析** (边界提取 + 直线拟合 + 梯形构建) |
| 实现接口 | `CorridorPredictorInterface` (抽象基类) |

#### 核心算法

1. **道路边界提取**：
   - 逐行扫描道路掩码 (步长 = 2)
   - 记录每行最左和最右的道路像素 x 坐标
   - 对缺失行进行线性插值填充

2. **直线拟合**：
   - 对左右边界点分别做 `np.polyfit(y, x, deg=1)` 线性拟合
   - 得到道路边缘方程：`x = a×y + b`
   - 计算消失点 (vanishing point)：左右边缘延长线交点

3. **梯形走廊构建**：
   - 走廊底边 = 道路底部实际宽度
   - 走廊宽度 = `corridor_width_ratio × 道路宽度` (以车头中心为基准)
   - 走廊顶边 = 从底边向消失点方向延伸 `corridor_top_ratio`
   - 边缘平行于道路边缘斜率

4. **风险计算**：
   - 几何风险：道路覆盖度 + 边界连续性
   - 方向风险：中心线偏移量 (弯道路检测)
   - 宽度风险：走廊面积/道路面积比
   - 综合置信度 = 1.0 - 加权风险 (0.4×几何 + 0.3×方向 + 0.3×宽度)

5. **时序平滑**：
   - 维护最近 10 帧历史缓冲区
   - 置信度衰减加权 (decay = 0.9)
   - 当前帧权重 0.6 + 历史均值权重 0.4

#### 走廊形状说明

| 条件 | 形状 | 说明 |
|------|------|------|
| `corridor_top_ratio < 1.0` | 梯形 (Trapezoid) | 底边宽、顶边窄，模拟透视效果 |
| `corridor_top_ratio >= 1.0` | 三角形 (Triangle) | 顶边收敛到消失点 |

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enabled` | bool | `true` | 模块开关 |
| `min_road_width` | int | 50 | 最小道路宽度 (像素) |
| `boundary_sample_step` | int | 2 | 边界采样步长 (行间隔) |
| `min_valid_rows` | int | 10 | 最小有效行数 |
| `corridor_width_ratio` | float | 0.8 | 走廊宽度占道路宽度比例 (default.yaml 配置值) |
| `corridor_top_ratio` | float | 0.8 | 走廊延伸比例 (向消失点) |
| `geometry_risk_weight` | float | 0.4 | 几何风险权重 |
| `direction_risk_weight` | float | 0.3 | 方向风险权重 |
| `width_risk_weight` | float | 0.3 | 宽度风险权重 |
| `temporal_window` | int | 5 | 时序平滑窗口 |
| `confidence_decay` | float | 0.9 | 历史置信度衰减因子 |
| `focal_length_px` | float | 1000.0 | 焦距 (用于预测距离计算) |
| `camera_height_m` | float | 1.50 | 相机高度 (用于预测距离计算) |
| `horizon_ratio` | float | 0.50 | 地平线比例 |
| `ego_speed_mps` | float | 13.9 | 自车速度 (m/s, 用于时间预测) |
| `prediction_horizon_seconds` | float | 3.0 | 预测时间窗口 |

#### 障碍物避让逻辑

```
corridor_mask (原始走廊)
    │
    ▼
  遍历所有已知 bbox + 未知 bbox
    │
    ▼
  在走廊中将障碍物区域像素清零 (cutout)
    │
    ▼
  输出 cutout_mask (可用于可视化避让区域)
```

#### 预测标记 (Prediction Markers)

沿中心线均匀采样 5 个点，计算每个点的：
- `distance_m`：基于几何投影的真实距离
- `time_s`：`distance_m / ego_speed_mps`，到达该点预估时间

#### 输出数据结构

```python
CorridorPredictionResult(
    corridor_mask: Optional[np.ndarray]  # 走廊二值掩码
    polygon: List[Tuple[int, int]]       # 梯形四角坐标 [(bl, br, tr, tl)]
    centerline: List[Tuple[int, int]]    # 中心线点序列
    confidence: float                    # 走廊置信度 (1.0=安全, 0.0=危险)
    inference_time_ms: float            # 处理耗时
    error_code: int                      # 错误码
    error_message: str                   # 错误信息
    method: str                          # "road_mask_geometry"
    model_version: str                   # "v1.0"
    is_enabled: bool                     # 是否启用
)
```

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_predict_corridor()` | **核心算法**：边界提取→直线拟合→梯形构建→风险计算 |
| `_extract_road_boundaries()` | 逐行提取左右道路边界 |
| `_interpolate_boundaries()` | 线性插值填充缺失边界行 |
| `_build_trapezoid_corridor()` | 直线拟合 → 构建梯形走廊 |
| `_build_corridor_mask()` | 多边形填充生成掩码 |
| `_analyze_direction()` | 弯道/直道方向分析 |
| `_estimate_geometry_risk()` | 道路几何风险估算 |
| `_estimate_width_risk()` | 走廊宽度风险估算 |
| `_compute_confidence()` | 加权置信度计算 |
| `_temporal_smooth_confidence()` | 时序平滑 |
| `_apply_obstacle_avoidance()` | 障碍物避让 (从走廊扣除障碍物区域) |
| `_compute_prediction_markers()` | 基于几何投影计算距离和时间标记 |
| `reset()` | 清空跨帧历史状态 |

#### 优点

- ✅ **纯几何方法**：无需模型推理，计算极快
- ✅ **可解释性强**：梯形走廊完全基于道路边界拟合
- ✅ **时序稳定**：历史缓冲区平滑置信度，防止跳变
- ✅ **自适应道路宽度**：走廊宽度随道路宽度变化
- ✅ **弯道检测**：通过中心线偏移检测弯道
- ✅ **障碍物避让**：可输出扣除障碍物后的安全走廊
- ✅ **预测信息**：输出沿路径的距离和时间标记
- ✅ **可独立关闭**：通过 `enabled: false` 禁用

#### 缺点

- 🟡 **严重依赖分割质量**：道路分割出错时走廊完全失效
- 🟡 **直线拟合假设**：弯道/复杂地形下直线拟合失准
- 🟡 **回退启发式**：拟合失败时使用对称梯形启发式，精度有限
- 🟡 **无动态更新**：`ego_speed_mps` 为静态配置，不能实时获取车速
- 🟡 **预测标记非真实预测**：基于静态速度的时间估计，非轨迹预测
- 🟡 **不考虑道路曲率**：仅支持直线道路建模

---

### 4.7 RiskEvaluator — 风险评估模块

#### 基本信息

| 属性 | 说明 |
|------|------|
| 源文件 | `src/modules/risk_evaluator.py` |
| 配置来源 | `configs/default.yaml` → `risk_evaluator` 段 |
| 算法 | **空间冲突检测 + TTC (Time-To-Collision) 估计 + 多帧跟踪** |
| 实现功能 | 综合输出每帧的风险等级和障碍物风险详情 |

#### 风险等级定义

| 等级 | 颜色 (BGR) | 含义 |
|------|-----------|------|
| `safe` | (0, 200, 0) 绿色 | 无障碍物在走廊内 |
| `notice` | (0, 255, 255) 黄色 | 有障碍物但无紧迫危险 |
| `warning` | (0, 140, 255) 橙色 | 存在碰撞风险 |
| `danger` | (0, 0, 255) 红色 | 即将碰撞 |

#### 核心算法流程

```
已知/未知障碍物
    │
    ▼
构建占用掩码 (footprint)
    │
    ▼
生成走廊缓冲带 (near_mask)
    │
    ▼
空间关系判定 ─────────────┐
  - intersecting (相交)    │
  - near (走廊附近)        │
  - outside (走廊外)       │
                           │
    ▼                      │
目标跟踪 (IoU 关联)       │
  - 关联历史轨迹           │
  - 距离历史存储           │
                           │
    ▼                      │
TTC 估计 ─────────────────┘
  - 距离历史线性拟合
  - R² 质量检查
  - closing speed 检查
  - TTC = distance / closing_speed
                           │
    ▼
风险分类
  - 空间关系 + 距离阈值
  - 空间关系 + TTC 阈值
  - 连续帧确认 (稳定帧数)
                           │
    ▼
最高风险选择
```

#### 关键参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `intersection_ratio` | float | 0.15 | 相交判定阈值 (障碍物在走廊内的占比) |
| `near_margin_ratio` | float | 0.025 | 走廊缓冲带宽度比 |
| `footprint_height_ratio` | float | 0.25 | 障碍物底部占用区高度比 |
| `max_corridor_lateral_ratio` | float | 0.65 | 接地点横向偏离比上限 |
| `min_known_confidence` | float | 0.45 | 已知障碍物最低置信度 |
| `max_known_bbox_area_ratio` | float | 0.40 | 已知障碍物最大 bbox 面积比 |
| `max_known_bbox_border_count` | int | 2 | 已知障碍物最大边数触边数 |
| `notice_distance_m` | float | 30.0 | 注意距离阈值 |
| `warning_distance_m` | float | 15.0 | 警告距离阈值 |
| `danger_distance_m` | float | 6.0 | 危险距离阈值 |
| `warning_ttc_s` | float | 3.0 | 警告 TTC 阈值 |
| `danger_ttc_s` | float | 1.5 | 危险 TTC 阈值 |
| `track_iou_threshold` | float | 0.30 | 跟踪 IoU 关联阈值 |
| `history_size` | int | 15 | 每个轨迹的历史帧数 |
| `confirm_frames` | int | 3 | 连续帧确认帧数 |
| `min_ttc_samples` | int | 8 | TTC 计算所需最小样本数 |
| `min_ttc_observation_s` | float | 0.25 | TTC 最小观测时间 |
| `min_ttc_r_squared` | float | 0.80 | TTC 线性拟合最小 R² |
| `min_closing_observations_ratio` | float | 0.75 | 距离减小观测占比下限 |
| `min_corridor_confidence` | float | 0.45 | 走廊最低可用置信度 |
| `min_closing_speed_mps` | float | 0.5 | 最小接近速度 |
| `max_closing_speed_mps` | float | 25.0 | 最大接近速度 |
| `max_track_gap_s` | float | 0.5 | 最大跟踪间隔 (秒) |

#### TTC 估计算法

```python
# 1. 收集距离历史 (至少 min_ttc_samples=8 个样本)
# 2. 计算时间序列和距离序列
# 3. 检查 closing_ratio (距离减小的占比 ≥ 0.75)
# 4. 线性拟合: distance = slope × time + intercept
# 5. R² 检查 (≥ 0.80)
# 6. closing_speed = -slope (正值表示接近)
# 7. closing_speed ∈ [0.5, 25.0] m/s 检查
# 8. TTC = current_distance / closing_speed
```

#### 风险分类逻辑

```
空间关系 = "outside" → safe

连续帧数 < confirm_frames → notice (unconfirmed)

空间关系 = "intersecting":
  ├── distance ≤ danger_distance_m → danger (critical_distance)
  ├── distance ≤ warning_distance_m + TTC ≤ danger_ttc_s → danger (critical_ttc)
  ├── distance ≤ warning_distance_m → warning (warning_distance)
  ├── distance ≤ notice_distance_m + TTC ≤ warning_ttc_s → warning (warning_ttc)
  ├── distance 不可用 → warning (distance_unavailable)
  └── 其他 → notice (far)

空间关系 = "near":
  ├── distance ≤ danger_distance_m 或 TTC ≤ warning_ttc_s → warning (near_close)
  └── 其他 → notice (near_corridor)
```

#### 输出数据结构

```python
RiskEvaluationResult(
    risk_level: str                  # "safe"/"notice"/"warning"/"danger"
    major_reason: str                # 主要原因描述
    obstacle_risks: List[ObstacleRisk]  # 每个障碍物的风险详情
    system_status: str               # normal/degraded/unavailable
    is_valid: bool                   # 结果是否有效
    inference_time_ms: float         # 评估耗时
    error_code: int                  # 错误码
    error_message: str               # 错误信息
    model_version: str               # "video-risk-v1.0"
    is_enabled: bool                 # 是否启用
)
```

每个 `ObstacleRisk`：
```python
ObstacleRisk(
    object_id: str                   # 轨迹 ID
    source: str                      # "known"/"unknown"
    class_name: Optional[str]        # 类别名 (unknown 为 None)
    bbox: Tuple[int, int, int, int]  # 边界框
    distance: Optional[float]        # 距离
    corridor_overlap: float          # 走廊交叠率
    spatial_relation: str            # "intersecting"/"near"/"outside"
    ttc: Optional[float]            # TTC (秒)
    risk_level: str                  # 单目标风险等级
    major_reason: str                # 单目标风险原因
    stable_frames: int               # 稳定帧数
)
```

#### 方法列表

| 方法 | 功能 |
|------|------|
| `_evaluate_obstacles()` | 统一评估已知+未知障碍物 |
| `_is_plausible_known_object()` | 已知障碍物合理性检查 |
| `_build_bbox_footprint()` | 构建 bbox 底部占用掩码 |
| `_build_near_mask()` | 生成走廊缓冲带 |
| `_compute_spatial_relation()` | 空间关系计算 (交叠率+接触点) |
| `_is_contact_in_mask()` | 接地点是否在走廊内 |
| `_associate_tracks()` | IoU 关联历史轨迹 |
| `_create_track()` | 创建新轨迹 |
| `_update_track()` | 更新已有轨迹 |
| `_prune_tracks()` | 清除超期轨迹 |
| `_estimate_ttc()` | 距离历史 → TTC 估计 |
| `_classify_obstacle()` | 单目标风险分类 |
| `_select_frame_risk()` | 选择全帧最高风险 |
| `_bbox_iou()` | 计算两个 bbox 的 IoU |
| `reset()` | 清空所有轨迹和状态 |

#### 优点

- ✅ **多维度风险评估**：空间关系 + 距离 + TTC + 连续帧
- ✅ **轻量级跟踪**：IoU 关联，无需复杂 ReID 模型
- ✅ **TTC 拟合质量检查**：R² ≥ 0.80 确保拟合可靠性
- ✅ **距离减小占比检查**：防止噪声导致的虚假 TTC
- ✅ **距离/速度双阈值**：同时考虑静态距离和动态 TTC
- ✅ **未知障碍物纳入评估**：统一处理 known + unknown 障碍物
- ✅ **连续帧确认**：需要 3 帧连续检测才升级风险
- ✅ **丰富的降级处理**：走廊不可用时系统降级

#### 缺点

- 🟡 **无自车动态模型**：TTC 基于障碍物静止假设 (closing_speed = -slope)
- 🟡 **轻量级跟踪局限**：IoU 关联在遮挡后易丢失目标
- 🟡 **无 ReID**：无法在目标移出画面后重新识别
- 🟡 **静态阈值**：风险阈值 (30m/15m/6m) 不随车速动态调整
- 🟡 **TTC 延迟**：至少需要 8 帧历史才能计算 TTC
- 🟡 **假设障碍物静止**：未考虑障碍物自身运动方向

---

## 5. 数据输入模块

### 5.1 VideoReader

| 属性 | 说明 |
|------|------|
| 源文件 | `src/data/video_reader.py` |
| 功能 | 从视频文件逐帧读取 |
| 支持跳帧 | `frame_skip` 参数，默认 1 (不跳) |
| 实现 | `cv2.VideoCapture` |

#### 关键方法

| 方法 | 功能 |
|------|------|
| `open()` | 打开视频文件 |
| `read()` | 读取单帧 (含跳帧逻辑) |
| `frames()` | 生成器，逐帧迭代 |
| `get_info()` | 获取 FPS/分辨率/总帧数 |
| `release()` | 释放资源 |

## 6. 可视化模块

### 6.1 result_visualizer.py

| 属性 | 说明 |
|------|------|
| 源文件 | `src/utils/result_visualizer.py` |
| 功能 | 将所有检测结果绘制到输出帧上 |
| 支持元素 | 道路掩码、走廊、异常掩码、已知/未知 bbox、风险信息 |

#### 绘制颜色方案

| 元素 | 颜色 (BGR) | 说明 |
|------|-----------|------|
| 道路掩码 | (0, 200, 0) 绿色通道 | 半透明叠加 |
| 走廊掩码 | (0, 0, 255) 红色通道 | 半透明叠加 + 橙色边框 |
| 走廊中心线 | (255, 255, 255) 白色 | 折线 |
| 异常掩码 | (255, 0, 0) 蓝色通道 | 半透明叠加 |
| 已知障碍物 bbox | 按风险等级变色 | 默认黄色 |
| 未知区域 bbox | 按风险等级变色 | 默认紫色 |

#### 风险等级颜色

| 等级 | BGR |
|------|-----|
| safe | (0, 200, 0) |
| notice | (0, 255, 255) |
| warning | (0, 140, 255) |
| danger | (0, 0, 255) |

#### 标签内容

每个检测框标签包含：`类别名 置信度 距离 空间关系 风险等级`

### 6.2 live_visualizer.py

| 属性 | 说明 |
|------|------|
| 源文件 | `src/utils/live_visualizer.py` |
| 功能 | 实时弹窗显示处理帧 |
| 交互 | 按 `q` 键退出 |
| 适用 | 仅摄像头模式 |

---

## 7. 统一数据结构（Schemas）

文件：`src/interface/schemas.py`

### 7.1 核心数据类

| 类名 | 用途 | 关键字段 |
|------|------|---------|
| `DetectedObject` | 单个已知障碍物 | class_name, bbox, confidence, distance |
| `KnownDetectionResult` | 已知检测整体结果 | objects[], inference_time_ms, model_version |
| `DistanceEstimationResult` | 距离估计结果 | known_objects[], method, is_enabled |
| `UnknownRegion` | 单个未知异常区域 | bbox, score, distance, object_id, area, mask_rle |
| `UnknownDetectionResult` | 未知检测整体结果 | score_map, anomaly_mask, regions[] |
| `ObstacleRisk` | 单障碍物风险详情 | object_id, source, ttc, risk_level |
| `RiskEvaluationResult` | 风险评估整体结果 | risk_level, obstacle_risks[], system_status, ttc |
| `RoadSegmentResult` | 道路分割结果 | mask, confidence_map, global_confidence, system_status |
| `CorridorPredictionResult` | 走廊预测结果 | corridor_mask, polygon, centerline, confidence |
| `FrameResult` | 单帧完整结果 | 汇总所有模块输出 |
| `SystemStatus` | 系统状态常量 | NORMAL / DEGRADED / UNAVAILABLE |

### 7.2 设计特点

- 所有结果类都有 `is_successful` 属性 (基于 `error_code == 0`)
- 支持 `is_enabled` 开关 (可独立关闭某些模块)
- 支持 `to_dict()` 方法 (便于序列化)
- TTC 和 corridor_overlap 作为聚合属性自动计算
- 统一的 `error_code` 体系：0=成功, -1=无效输入, -2=模型加载失败, -3=推理错误

---

## 8. 模块间接口（Interfaces）

文件：`src/interface/module_interfaces.py`

### 8.1 DistanceEstimatorInterface

```python
class DistanceEstimatorInterface(ABC):
    @abstractmethod
    def estimate(
        self,
        frame: np.ndarray,
        frame_id: int,
        known_objects: List[DetectedObject],
    ) -> DistanceEstimationResult:
        ...
```

### 8.2 CorridorPredictorInterface

```python
class CorridorPredictorInterface(ABC):
    @abstractmethod
    def predict(
        self,
        frame: np.ndarray,
        frame_id: int,
        road_mask: np.ndarray,
    ) -> CorridorPredictionResult:
        ...

    @abstractmethod
    def reset(self) -> None:
        ...
```

### 8.3 接口设计原则

- **稳定抽象**：接口定义固定，内部实现可自由替换
- **不修改 schemas**：数据结构不可修改（项目硬约束）
- **单一职责**：每个接口仅规定该模块必须实现的方法
- **向后兼容**：新增参数使用 `Optional` 和默认值

---

## 9. 项目优缺点总览

### 9.1 整体优点

| 方面 | 说明 |
|------|------|
| **模块化设计** | 7 个独立模块，接口清晰，可单独替换和测试 |
| **统一数据结构** | 通过 schemas.py 定义统一数据格式，模块间解耦 |
| **优雅降级** | normal → degraded → unavailable 三级降级，保证系统鲁棒性 |
| **双模态检测** | 已知 (YOLO) + 未知 (Mask2Anomaly) 互补，覆盖全面 |
| **完整风险流水线** | 检测 → 距离 → 走廊 → TTC → 风险，形成完整链路 |
| **可配置性** | 所有阈值通过 YAML 配置，无需改代码 |
| **权重安全** | SHA256 校验，防止权重文件损坏 |
| **时序稳定** | 多帧历史缓冲 + 滑动平均，防止逐帧跳变 |
| **完善的日志** | 每帧详细 JSON 记录，便于回放和分析 |
| **代码规范** | 遵循 PEP 8，类型标注，中文注释 |

### 9.2 整体缺点

| 方面 | 说明 |
|------|------|
| **单线程串行** | 所有模块串行执行，无并行/异步优化 |
| **无 GPU 流水线** | 各模块各自管理 GPU 内存，无统一调度 |
| **几何距离局限** | 纯几何方法，依赖精确内参和高度先验 |
| **无目标跟踪器** | 无 DeepSORT/ByteTrack 等成熟跟踪算法 |
| **YOLO 仅 4 类** | 自训练模型，类别覆盖有限 |
| **Mask2Anomaly 依赖重** | Detectron2 + CUDA 编译链，部署门槛高 |
| **配置分散** | 参数分散在多个 YAML 文件中，维护成本高 |
| **无实时性能保障** | SegFormer + Mask2Anomaly 合计 ~400-900ms/帧 |
| **缺乏单元测试覆盖** | 测试文件存在但覆盖度不明确 |
| **无 CLI 参数支持** | 仅支持 `python -m src.main` + 硬编码路径 |

### 9.3 优化建议

| 优先级 | 方向 | 具体建议 |
|--------|------|---------|
| 🔴 高 | 性能优化 | 考虑使用 TensorRT 加速 YOLO 和 SegFormer 推理 |
| 🔴 高 | 并行化 | 将 RoadSegmenter 和 KnownDetector 并行执行 |
| 🟡 中 | 跟踪升级 | 引入 DeepSORT 或 ByteTrack 替代当前 IoU 关联 |
| 🟡 中 | 深度估计 | 可考虑引入轻量深度估计模型 (如 Depth Anything) |
| 🟡 中 | 类别扩展 | 扩充 YOLO 训练数据，增加更多障碍物类别 |
| 🟢 低 | 统一配置 | 将分散的 YAML 配置整合成单一配置体系 |
| 🟢 低 | CLI 支持 | 添加 argparse 支持命令行参数 |
| 🟢 低 | Web 界面 | 考虑添加 Web 界面进行实时监控 |

---

## 10. 依赖与环境

### 10.1 Python 依赖

| 包名 | 版本要求 | 用途 |
|------|---------|------|
| `numpy` | ≥ 1.24.0 | 数值计算 |
| `opencv-python` | ≥ 4.8.0 | 图像处理、视频读写 |
| `torch` | ≥ 2.0.0 | 深度学习框架 |
| `torchvision` | ≥ 0.15.0 | 视觉模型组件 |
| `transformers` | ≥ 4.35.0 | HuggingFace SegFormer |
| `ultralytics` | ≥ 8.3.0 | YOLOv8 检测 |
| `scipy` | ≥ 1.10.0 | B 样条插值 |
| `pyyaml` | ≥ 6.0.0 | YAML 配置解析 |
| `detectron2` | 0.6 (源码安装) | Mask2Former 后端 |
| `fvcore` | ≥ 0.1.5 | Detectron2 依赖 |
| `iopath` | ≥ 0.1.9 | 文件路径工具 |

### 10.2 推荐环境版本

| 组件 | 版本 |
|------|------|
| Python | 3.10 |
| CUDA Toolkit | 11.8 |
| PyTorch | 2.0.1+cu118 |
| Torchvision | 0.15.2+cu118 |
| NumPy | 1.26.4 |
| OpenCV | 4.10.0.84 |
| Transformers | 4.36.2 |
| Detectron2 | 0.6 (本地源码) |

### 10.3 模型权重清单

| 模块 | 权重文件 | 格式 | 大小 (估) |
|------|---------|------|----------|
| SegFormer 道路分割 | `checkpoints/segformer_mit-b2_cityscapes.pth` | PyTorch .pth | ~140 MB |
| YOLO 已知检测 | `checkpoints/yolo_best.pt` | PyTorch .pt | ~25 MB |
| Mask2Anomaly 异常检测 | `checkpoints/mask2anomaly/best_contrastive_fp16.pth` | PyTorch .pth | ~100 MB |

---

## 附录 A：项目脚本

### A.1 download_checkpoint.py

| 属性 | 说明 |
|------|------|
| 路径 | `scripts/download_checkpoint.py` |
| 功能 | 从 HuggingFace Hub 下载 SegFormer 预训练权重 |
| 支持模型 | b0 / b1 / b2 / b3 / b4 / b5 (Cityscapes) |
| 下载方式 | 优先使用 `segmentation-models-pytorch.from_pretrained()`，失败回退到 `huggingface_hub.hf_hub_download()` |
| 用法 | `python scripts/download_checkpoint.py --model segformer_mit-b2_cityscapes` |

### A.2 export_mask2anomaly_fp16.py

| 属性 | 说明 |
|------|------|
| 路径 | `scripts/export_mask2anomaly_fp16.py` |
| 功能 | 将 Mask2Anomaly FP32 权重导出为 FP16 推理权重，并计算 SHA256 |
| 处理流程 | 加载检查点 → 转换浮点参数为 FP16 → 保存 → 输出 SHA256 |
| 用法 | `python scripts/export_mask2anomaly_fp16.py --input original.pth --output fp16.pth` |

### A.3 evaluate.py

| 属性 | 说明 |
|------|------|
| 路径 | `scripts/evaluate.py` |
| 功能 | 评估 Mask2Former 模型在 Cityscapes 验证集上的性能 |
| 依赖 | Detectron2 评估工具 |

### A.4 preprocess_cityscapes.py

| 属性 | 说明 |
|------|------|
| 路径 | `scripts/preprocess_cityscapes.py` |
| 功能 | Cityscapes 数据集预处理（用于训练 Mask2Former） |
| 操作 | 数据格式转换、图像缩放、标签处理 |

---

> **文档维护者**：国创视觉+ 

> **技术参数文档** 是项目技术细节的权威参考，任何模块的修改都应同步更新本文档。
